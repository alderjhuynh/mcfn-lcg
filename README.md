So I was bored and made a linear congruential generator, even though minecraft already has `/random`.

you can call the function with function `lcg:lcg {"upper":5,"lower":1}`, where 5 and 1 are just the upper and lower bounds you wanna choose

or just run /random
